<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="emad.android.cleanarchitechernoteapptheme">

    <application
        android:name=".NoteApp"
        android:allowBackup="true"
        android:icon="@drawable/blanknote"
        android:label="@string/app_name"
        android:roundIcon="@drawable/blanknote"
        android:supportsRtl="true"
        android:theme="@style/Theme.CleanArchitecherNoteAppTheme">
        <activity
            android:name=".feature_note.presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>